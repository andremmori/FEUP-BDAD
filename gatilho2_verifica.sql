SELECT SONG.idSong, SONG.length
from SONG
WHERE SONG.idSong = 1;

UPDATE SONG SET length = -21 WHERE SONG.idSong = 1;

SELECT SONG.idSong, SONG.length
from SONG
WHERE SONG.idSong = 1;

UPDATE SONG SET length = 4 WHERE SONG.idSong = 1;